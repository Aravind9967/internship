spring:
  datasource:
    url: jdbc:mysql://localhost:3306/telehealth_db
    username: root
    password: Aravind@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

screening:
  image:
    storage-dir: ./storage/screening-images
  ai:
    classifier-url: http://localhost:8000        # ML engineer's service